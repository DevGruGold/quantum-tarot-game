import { majorArcana } from './cards/majorArcana';
import { frequencies } from './cards/quantumFrequencies';

export const tarotDeck = {
  major: majorArcana,
  frequencies
};

export { getQuantumHarmony } from './cards/quantumFrequencies';